# 出張申請おたすけ君

自家用車での出張距離を測定し、通勤経路と重複する区間を除算して、出張申請に必要な地図を印刷するためのWebアプリです。**Google CloudのAPIキー・課金設定は不要**です。

- 地図描画：OpenStreetMap（Leaflet）
- 住所・施設検索：**Googleジオコーダ（GAS経由）** ＋ 国土地理院 ＋ OpenStreetMap（Nominatim）を統合し、精度の高い順に表示
  - 施設名で良い結果が出ない（0件 or 市の中心など広域だけ）ときは、**OSMのPOI検索（Overpass API）** で周辺の施設を名前から補完します（無料・キー不要）。
  - ※ GASの無料Mapsサービスはジオコーダ（住所中心）まで。Googleマップアプリのような施設名検索（Places）はAPIキー＋課金が必要なため使えません。住所での検索がもっとも確実です。
- ルート検索：**Googleマップ（Google Apps Script の Maps サービス経由）**
  - フロント（HTML）から GAS のウェブアプリAPIを叩くハイブリッド構成。GASの内蔵Mapsサービスを使うため、Google CloudのAPIキーは不要です。
  - GASに接続できない場合は、直線距離×1.3の推定値に自動フォールバックします。

## 仕組み（なぜ重複区間を引くのか）
通勤手当は「自宅 ↔ 勤務地」の通勤経路に対して既に支給されています。
そのため出張のとき、出張経路のうち通勤経路と重なる区間は二重に支給されないよう距離から差し引きます。

**申請距離 ＝ 出張経路の総距離 − 通勤経路との重複区間**

このアプリは、出張経路と通勤経路（自宅↔勤務地）を実際の道のりで計算し、
重なる区間を自動検出して地図上に強調表示しながら、申請に出す距離を算出します。

## セットアップ（経路検索の有効化）
ルート検索に Googleマップを使うため、先に GAS（Google Apps Script）バックエンドをデプロイします。

1. [script.google.com](https://script.google.com) で新規プロジェクトを作成
2. リポジトリの `コード.gs` の内容を貼り付けて保存
3. 「デプロイ」→「新しいデプロイ」→ 種類「**ウェブアプリ**」
   - 次のユーザーとして実行：**自分**
   - アクセスできるユーザー：**全員**
4. 発行された「ウェブアプリURL（`.../exec`）」をコピー
5. `index.html` 内の `const GAS_API_URL = '...'` にそのURLを貼り付け

> 未設定でもアプリは起動しますが、距離は直線距離×1.3の推定値になります。

## 使い方
ブラウザ（Edge / Chrome）で `index.html` を開くだけです。

1. **【最初の1回だけ】通勤ルートの確定**：自宅・勤務場所（学校）を登録し、「通勤ルート（初期設定）」でいつもの通勤ルートを1本確定します。提案ルートが実際の通勤路と違う場合は、「🖊 手動で調整」から地図をクリックして経由地を足し、実際に通る道に合わせて保存できます。確定した通勤ルートはブラウザに保存され、以降ずっと基準として使われます（自宅・学校を変えたときだけ再設定）。
2. **毎回の出張**：用務地を登録（**複数可**。上から回る順に並び、▲▼で並べ替え／「🔀最短の順に並べ替え」で自動最適化）→ 出張パターンを選択 → 距離を計算。確定済みの通勤ルートと重なる区間が自動検出され、その分を差し引いた申請距離が区間ごと（順路１・２・３…）に出ます → ルートを決定 → 地図を印刷。
   - 用件欄に貼る文章（例：`○○会議（順路１：1.5km、２：3.2km、３：4.0km） ※直行（自宅住所）`）も自動生成されます。

### 出張パターン
- ① 学校 → 用務地 → 学校（除算なし）
- ② 自宅 → 用務地 → 学校（行きの通勤経路と重複する区間を除算）
- ③ 学校 → 用務地 → 自宅（帰りの通勤経路と重複する区間を除算）
- ④ 自宅 → 用務地 → 自宅（行き帰りの通勤経路と重複する区間を除算）

## 公開方法

### A. GASでアプリごと配信（学校などGitHubが開けない環境向け・推奨）
学校PCは `script.google.com` だけ見られればよく、HTMLの取得はGAS（Googleのサーバー）が代行します。
**巨大HTMLを手で貼り付ける必要はありません。**
1. GitHubリポジトリを **Public** にする（GASがHTMLを読みに行くため）
2. GAS プロジェクトに `コード.gs` を貼り付け、`APP_HTML_URL` が自分のリポジトリの raw URL になっているか確認
   （例：`https://raw.githubusercontent.com/edupower07/maps/main/index.html`）
3. デプロイ（ウェブアプリ／**実行=自分**／**アクセス=Googleアカウントを持つ全員**）。初回は UrlFetch の権限を許可
   - ⚠ アクセスを「**全員（匿名ユーザーを含む）**」にすると利用者のアカウントを識別できず、
     利用制限を有効にしたとき全員が拒否されます
   - アプリ本体の取得・ルート検索は `google.script.run`（Apps Script公式の通信方式）で行うため、
     ログインが必要な設定でも正しく動作します
4. 発行された `.../exec` を開くと **アプリ画面が表示**される（GASが `index.html` を取得して配信）
5. Googleサイトに、その exec URL を iframe で埋め込む：
   ```html
   <iframe src="https://script.google.com/macros/s/XXXX/exec" width="100%" height="900" frameborder="0"></iframe>
   ```
   （Googleサイト → 挿入 → 埋め込む → 「コードを埋め込む」に貼り付け）

> `index.html` を更新すると、最大10分ほどで配信内容も自動更新されます（手貼り不要）。
> すぐ反映したいときは GAS で `clearAppHtmlCache()` を実行してください。
> （`コード.gs` を変更したときだけ、貼り替えと再デプロイが必要です）
> 印刷は埋め込み枠内だと不安定なので、印刷時は exec URL を直接開いて使ってください。

### B. GitHub Pages（リポジトリがPublicの場合）
**Settings → Pages** で Branch を `main` ／ フォルダを `/ (root)` にすると、
`https://<ユーザー名>.github.io/<リポジトリ名>/` で公開されます。

## 利用制限（アクセス制御）
**利用申請をした人だけが使えます。** 申請者は1件ずつ手で追加する必要はなく、
フォームに回答が入った時点で自動的に利用可能になります（反映は最大5分）。

判定の順番：
1. `RESTRICT_TO_DOMAINS` 以外のドメイン → **拒否**（申請していても使えない）
2. `ADMIN_EMAILS`（管理者）→ 許可
3. `AUTO_ALLOW_DOMAINS`（申請不要にしたい場合のみ。通常は空）→ 許可
4. **申請フォームの回答シートに載っている** → 許可
5. どれにも当てはまらない → 「利用を申請する」案内画面

現在の設定：`RESTRICT_TO_DOMAINS = ['kita9.ed.jp']`（北九州市の学校アカウント**かつ申請済み**の人のみ利用可）。

### 申請フォームの作成（GASで自動生成）
GASエディタで関数 **`setupApplicationForm`** を1回実行するだけです。次が自動で作られます。
- Googleフォーム（**お名前／学校名／役職**。メールアドレスはログイン中のアカウントを自動収集）
- 回答スプレッドシート（＝申請者一覧・そのまま許可リストになる）
- 設定の保存（`APPLY_FORM_URL` / `ALLOWLIST_SHEET_ID` をスクリプト プロパティへ）

実行結果に表示されるフォームURLを職員に案内してください。
`listRegisteredUsers()` を実行すると、現在利用できるアカウントの一覧を確認できます。

### ⚠ 有効にする前に必ず確認（重要）
利用者のメールアドレスを取得できるのは、**GASの所有者と利用者が同じドメイン**の場合だけです。
個人のGmailでGASを作っていると、`kita9.ed.jp` の先生のアドレスが取得できず**全員が拒否**されます。

デプロイ後、**先生のアカウントで** `<exec URL>?whoami=1` を開いて確認してください。

| 結果 | 意味 |
|---|---|
| `"email":"…@kita9.ed.jp", "allowed":true` | ✅ そのまま使えます |
| `"email":"(取得できませんでした)"` | ❌ まずデプロイの「アクセスできるユーザー」を **Googleアカウントを持つ全員**（または組織内の全員）に変更して再デプロイ。それでも取得できない場合は、GASを `kita9.ed.jp` のアカウントで作り直す |

### 設定手順
1. `コード.gs` を最新にする（`RESTRICT_TO_DOMAINS = ['kita9.ed.jp']` を確認）
2. 自分を管理者にする：GASの「プロジェクトの設定 → スクリプト プロパティ」に
   キー `ADMIN_EMAILS` ／ 値 自分のメールアドレス（カンマ区切りで複数可）を登録
   ※ このリポジトリは公開のため、個人のアドレスはコードに直接書かないこと
3. GASエディタで **`setupApplicationForm`** を実行（フォームと回答シートが自動生成される）
4. 再デプロイ（デプロイを管理 → 鉛筆 → 新バージョン）
5. 職員に配るのは **アプリのURLだけ**。未申請の人が開くと申請フォームへのボタンが
   自動で表示されるので、フォームURLを別に配る必要はありません
   （`setupApplicationForm` の実行結果に、そのまま使えるお知らせ文も出力されます）

| 設定 | 意味 |
|---|---|
| `ALLOWED_DOMAINS` | このドメインのアカウントは申請不要で許可（自治体ドメインなど） |
| `ALLOWLIST_SHEET_ID` | 申請フォームの回答シート。載っているアドレスを自動で許可 |
| `ALLOWLIST_APPROVED_HEADER` | 列名を指定すると「承認」列が `TRUE/○/承認` の行だけ許可（管理者承認制） |
| `ADMIN_EMAILS` | 常に利用できる管理者 |

- 申請の反映は最大5分（キャッシュ）。すぐ反映したいときは GAS で `clearAllowlistCache()` を実行。
- 利用状況の確認：
  - `showUserUsage()` … **アカウント別**の利用回数（誰が何回使ったか。既定30日、`showUserUsage(7)` で期間変更）。
    記録は申請者名簿と同じスプレッドシートの「利用ログ」シートに、日時・メールアドレス・操作・用務地の数が追記されます
    （**行き先の住所は記録しません**）。
  - `showUsage()` … 1日あたりのGoogleマップ呼び出し回数（直近14日）が見られます。
  目安として **1件の出張申請＝5〜6回**（用務地1か所）、通勤ルートの初期設定＝約4回を消費します。
  GASのMapsサービスには1日あたりの上限があるため、上限に近づく場合は学校ごとにGASを分けてください。
- デプロイの「アクセスできるユーザー」を **組織内の全員** にすると、組織外を完全に遮断できます（併用推奨）。
- 画面表示時に本人確認し、以降の通信は署名付きトークン（12時間有効）で検証するため、URLの直叩きもできません。

## フォルダ構成
- `index.html` … アプリ本体（GASが自動取得して配信／GitHub Pages・ローカルでも開ける）
- `出張申請おたすけ君.html` … 同じ本体（ローカルで開く用）
- `コード.gs` … アプリ配信＋経路/住所検索API（Google Apps Script にデプロイして使う）
- `使い方マニュアル.html` / `使い方マニュアル.pdf` … 操作マニュアル

## 注意
- 保存（自宅・勤務地・よく使う用務地）は、使っているブラウザ内に保存されます（端末間の自動同期はありません）。
- 住所検索・地図タイルは公開API（https）を利用します。ネットワークが `openstreetmap.org` / `unpkg.com` / `gsi.go.jp` への接続を許可している必要があります。
- ルート検索は GAS のウェブアプリ（`script.google.com` / `script.googleusercontent.com`）への接続を許可している必要があります。
- 重複区間の判定（v2.1）：出張経路と通勤経路を約25m間隔に補間して比較し、「通勤経路から約45m以内」かつ「進行方向がほぼ平行」の部分を重複と判定します（120m未満の途切れは接続、60m未満の断片は除外）。通勤経路をただ横切るだけの交差点は重複になりません。実際と違う場合は結果画面の「✂ 重複区間の調整」でON/OFFできます。
- ルート提案（v2.1）：出発時刻を「現在」としてGoogleの交通状況込みで検索するため、Googleマップの提案に近いルートが出ます。各候補・計算結果には「🗺 Googleマップで確認」リンクがあり、同じ区間をGoogleマップで開いて照合できます。
- ルートの形状（v2）：Googleの各ステップの詳細ポリラインを連結した高解像度の線を使います（`コード.gs` の更新＋再デプロイが必要）。`コード.gs` を更新したときは「デプロイ → デプロイを管理 → 鉛筆 → 新バージョン」で再デプロイしてください（URLは変わりません）。
